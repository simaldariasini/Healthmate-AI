services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: healthmate
      POSTGRES_USER: healthmate
      POSTGRES_PASSWORD: healthmate
    ports:
      - "5432:5432"
    volumes:
      - healthmate_pgdata:/var/lib/postgresql/data
volumes:
  healthmate_pgdata:
